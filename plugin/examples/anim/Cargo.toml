[package]
name = "anim"
version = "0.1.0"
edition = "2024"

[workspace]

[workspace.lints]

[package.metadata.component]
package = "component:anim"

[package.metadata.component.target]
path = "../../wit/caldrayne.wit"
world = "animation-plugin"

[package.metadata.component.dependencies]

[dependencies]
vek = "0.17.0"
wit-bindgen = { version = "0.51.0", features = ["macros"] }
wit-bindgen-rt = "0.30.0"

[lib]
crate-type = ["cdylib"]
