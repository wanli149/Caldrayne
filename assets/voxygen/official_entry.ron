// Bundled public-mode entry source.
// Phase 1 treats this file as the single official entry source for the client.
// Before a formal domain exists, this may temporarily point to an internal or test IP.
// Future launcher/bootstrap flows may replace this source, but they must still
// feed the same entry-policy layer instead of bypassing it with ad-hoc runtime
// server selection. Lightweight discovery or query-server hints may describe
// this realm, but they must not replace this source as the public runtime
// target authority.
(
    display_name: "Official Realm (LAN Test)",
    server_address: "192.168.1.8:14004",
    auth_server: None,
    use_srv: false,
    use_quic: false,
    validate_tls: true,
    source_kind: bundled,
)
